export class Task
{
    constructor(host, id, title, description, dueDate)
    {
        this.id = id;
        this.title = title;
        this.description = description;
        this.dueDate = dueDate;
        this.host = host;
    
    }

    crtajTask(host)
    {

    }
}